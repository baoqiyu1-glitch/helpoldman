server:
  port: 8080
  # servlet:
    # context-path: /api

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/helpoldman?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

# 在配置文件中添加
jwt:
  secret: helpoldmankey
  expiration: 86400000  # 24小时